{
  "name": "rhuma",
  "version": "1.0.0",
  "description": "Rhuma - Rhum di Corsica",
  "main": "index.js",
  "author": "Jean Hugues Robert",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon --watch streamlit_app.py index.js",
    "test": "jest",
    "install-python": "pyenv install 3.8.0 && pyenv global 3.8.0 && pip install pipenv && pipenv install -r requirements.txt",
    "install-node": "nodeenv -p --prebuilt && npm install"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "commander": "^13.1.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2"
  }
}
